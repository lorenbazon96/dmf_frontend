# Development defaults to http://localhost:3000/api when this is omitted.
# Production defaults to same-origin /api; set this only for a separate API origin.
VUE_APP_API_URL=https://api.example.com/api
